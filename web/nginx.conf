user www-data; worker_processes auto; pid /run/nginx.pid; events { worker_connections 768; } http { include /etc/nginx/mime.types; default_type application/octet-stream; access_log /var/log/nginx/access.log; error_log /var/log/nginx/error.log; server { listen 80; server_name confio.lat www.confio.lat; return 301 https://confio.lat$request_uri; } server { listen 443 ssl; server_name confio.lat www.confio.lat; root /var/www/html; index index.html; ssl_certificate /etc/letsencrypt/live/confio.lat/fullchain.pem; ssl_certificate_key /etc/letsencrypt/live/confio.lat/privkey.pem; include /etc/letsencrypt/options-ssl-nginx.conf; ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; location / { try_files $uri /index.html; } } }
