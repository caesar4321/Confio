[profile.default]
src = "."
test = "test"
script = "script"
out = "out"
cache_path = "cache"
libs = ["node_modules", "lib"]
allow_paths = ["../cusd_plus"]
optimizer = true
optimizer_runs = 200
solc = "0.8.26"

[fuzz]
runs = 512
