[profile.default]
src = "."
test = "test"
out = "out"
cache_path = "cache"
libs = ["node_modules", "lib"]
optimizer = true
optimizer_runs = 200
# Draft targets ^0.8.24; pin one compiler for reproducible builds
solc = "0.8.26"

[fuzz]
runs = 512
