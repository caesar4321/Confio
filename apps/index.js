/**
 * @format
 */

// Bootstrap loads polyfills first, then App
import './bootstrap';

// Enable screens and background messaging are handled in bootstrap
// to avoid NativeEventEmitter initialization issues