diff --git a/node_modules/react-native-vision-camera/android/CMakeLists.txt b/node_modules/react-native-vision-camera/android/CMakeLists.txt
index 17097c1..d1b35ba 100644
--- a/node_modules/react-native-vision-camera/android/CMakeLists.txt
+++ b/node_modules/react-native-vision-camera/android/CMakeLists.txt
@@ -80,9 +80,42 @@ endif()
 message("VisionCamera: Frame Processors: ${ENABLE_FRAME_PROCESSORS}!")
 if (ENABLE_FRAME_PROCESSORS)
     message("VisionCamera: Linking react-native-worklets...")
-    find_package(react-native-worklets-core REQUIRED CONFIG)
-    target_link_libraries(
-            ${PACKAGE_NAME}
-            react-native-worklets-core::rnworklets
-    )
+    
+    # Add path to find worklets-core
+    set(WORKLETS_CORE_DIR "${NODE_MODULES_DIR}/react-native-worklets-core/android/build/intermediates/cxx")
+    
+    # Try to find worklets-core package
+    find_package(react-native-worklets-core CONFIG PATHS ${WORKLETS_CORE_DIR})
+    
+    if(react-native-worklets-core_FOUND)
+        target_link_libraries(
+                ${PACKAGE_NAME}
+                react-native-worklets-core::rnworklets
+        )
+    else()
+        # Fallback: manually link to worklets library
+        message(WARNING "VisionCamera: Could not find react-native-worklets-core package, trying manual linking...")
+        
+        # Add worklets include directories
+        target_include_directories(
+                ${PACKAGE_NAME}
+                PRIVATE
+                "${NODE_MODULES_DIR}/react-native-worklets-core/cpp"
+        )
+        
+        # Link to the worklets library directly
+        find_library(
+                WORKLETS_LIB
+                rnworklets
+                PATHS "${NODE_MODULES_DIR}/react-native-worklets-core/android/build/intermediates/cmake/debug/obj/${ANDROID_ABI}"
+                      "${NODE_MODULES_DIR}/react-native-worklets-core/android/build/intermediates/cxx/Debug/*/obj/${ANDROID_ABI}"
+                NO_DEFAULT_PATH
+        )
+        
+        if(WORKLETS_LIB)
+            target_link_libraries(${PACKAGE_NAME} ${WORKLETS_LIB})
+        else()
+            message(FATAL_ERROR "VisionCamera: Could not find react-native-worklets-core library! Please make sure react-native-worklets-core is installed and built.")
+        endif()
+    endif()
 endif()
diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraDevicesManager.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraDevicesManager.kt
index 21ac2b2..ab2b21f 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraDevicesManager.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraDevicesManager.kt
@@ -99,6 +99,10 @@ class CameraDevicesManager(private val reactContext: ReactApplicationContext) :
   }
 
   fun sendAvailableDevicesChangedEvent() {
+    if (!reactContext.hasActiveReactInstance()) {
+      Log.w(TAG, "Skipping event emission - React context not ready")
+      return
+    }
     val eventEmitter = reactContext.getJSModule(RCTDeviceEventEmitter::class.java)
     val devices = getDevicesJson()
     eventEmitter.emit("CameraDevicesChanged", devices)
